# Codex 工作规则

本文件是当前工作空间 `/home/zero/mrdvs_ros2_ws` 的用户规则，适用于本目录及其子目录。Codex 在本工作空间内工作时必须优先读取并遵循这些规则。

## 1. 沟通语言

- 始终使用中文与用户沟通。

## 2. 需求理解与沟通

- 开始任务前，先理解用户的真实需求、使用场景和目标效果，再思考方案。
- 如果用户是在询问方案、讨论思路、比较技术路线，不要直接修改代码，必须先和用户沟通，等用户确认后再执行。
- 用户问问题时，要针对用户的具体疑惑直接解释，不要泛泛而谈。

## 3. Skill、插件与工作流检查

- 开始任务前，优先快速检查是否有适合当前任务的 Skill、插件或既有工作流。
- 如果有，需要先说明使用哪个 Skill 或工作流，以及为什么适合当前任务。

## 4. 项目理解

- 如果是在已有项目、已有逻辑或已有代码基础上修改，必须先阅读 `README.md`、相关文档和关键代码，理解项目结构、运行逻辑和现有功能。
- 不要在不了解项目上下文的情况下直接改代码。
- 上下文压缩或中断后继续执行时，必须先重新明确当前目标、任务计划、代码状态和开发记录，再继续工作，避免方向偏移。

## 5. 代码结构与质量

- 代码必须结构清晰、职责分离，不要把所有逻辑堆在一个文件里。
- 优先采用高质量、鲁棒、可维护的实现方案，而不是只追求临时方案。
- 如果任务本身可以简单解决，可以采用简洁方案，但不能牺牲后续维护性。
- 新功能应尽量融入现有代码风格、目录结构和设计模式。

## 6. README 与开发记录

- 最终总结时，用中文口述说明修改了什么、实现了什么效果、如何使用，不要额外生成一堆 Markdown 文件。
- 项目中只保留一个 `README.md` 作为说明和更新记录。
- 如果项目没有 `README.md`，必须在 `README.md` 中记录：
  - 工具用途
  - 核心逻辑
  - 代码结构
  - 使用方法
  - 更新记录
- 如果是在已有工具中新增功能，可以在 `README.md` 中补充必要的开发记录和使用说明。

## 7. 验证与排错

- 修改完成后，必须检查代码是否存在语法错误、运行错误、依赖问题或明显 bug。
- 必须检查新代码是否可能与已有功能冲突。
- 如果项目有测试、构建、lint 或启动命令，应尽量运行验证。
- 如果无法验证，需要明确告诉用户原因和潜在风险。

## 8. Git 提交与仓库管理

- 每次完成一个可运行、可回退的阶段后，需要提交到 Git。
- 提交前必须先检查当前项目涉及几个 Git 仓库，分别确认状态。
- 不要提交与当前任务无关的改动。
- 提交信息要清楚说明本次修改内容，方便后续回退和维护。
- 如果存在多个仓库，需要分别提交，并在总结中告诉用户提交了哪些仓库。

## 9. 多 Agent 工作方式

- 默认工作方式应优先考虑多 Agent 或子任务并行工作流。
- 当任务可以拆分为独立模块，例如代码阅读、方案设计、测试验证、UI 检查、文档整理时，应尽量并行处理以提高效率。
- 如果任务很小，或并行会增加复杂度，可以直接单 Agent 完成。
- 多 Agent 结果需要由主 Agent 统一汇总、判断和整合，不能简单拼接结论。

## 10. 终端与命令安全

- 发布或运行命令前，必须确认命令格式正确，避免因为语法错误导致终端卡住。

## 11. 最终反馈

- 任务完成后，用中文简洁总结：
  - 做了哪些修改
  - 会产生什么效果
  - 验证了哪些内容
  - 是否有未完成项或风险
  - Git 是否已提交，以及提交位置
- 不要写空泛总结，要让用户能快速知道当前项目发生了什么变化。
